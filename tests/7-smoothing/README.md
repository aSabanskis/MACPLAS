# Field smoothing test

Uses specialized class ```DoFFieldSmoother``` to smooth multiple FE fields at DoFs. Multiple iterations are performed according to the time parameters for the temperature solver.
